# 공부내용 



> ⚡10818번 | 최소, 최대
> ------------
>  풀이
```
몇 개의 정수를 입력 받는지를 Scanner로 입력받아주고 그에 따른 동적 배열을 하나 생성해줍니다.
그 다음 for문으로 동적 배열에 하나하나 값을 넣어주고 sort함수로 배열을 오름차순으로 정렬해줍니다.
이렇게 되면 맨 처음의 인덱스 값은 최솟값이 나오고 맨 마지막 인덱스의 값은 최댓값이 나오게 됩니다.
```

> ⚡2562번 | 최댓값
> ------------
>  풀이
```
문제에서 서로 다른 9개의 자연수라고 하였으므로 배열의 size를 9로 지정해주고 for문으로 배열의
안에 값을 넣어줍니다. 그리고 max의 값을 처음에는 배열의 첫 번째 인덱스의 값으로 해주고 인덱스 변수도
하나 선언해주고 값을 0으로 넣어줍니다. for문으로 최댓값을 구하는 공식을 if문 조건에 걸어줍니다.
max의 값이 바뀌면 인덱스의 값도 바뀌게 조건을 넣어줍니다. 
```

> ⚡2577번 | 숫자의 개수 
> ------------
>  풀이
```
3개의 정수를 곱하여 곱한 결과값에서 0부터 9의 숫자가 몇 번 쓰여졌는지를 묻는 문제입니다.
먼저 3개의 정수를 곱하는 것이므로 3개의 정수를 입력받아 곱한 결과의 값을 value라고 하며 int형 변수로
선언해줍니다. 이중 for문을 사용해주는데 바깥쪽의 for문은 0부터 9까지의 숫자를 비교해야 하므로 바깥쪽 for문에는
0부터 9까지의 조건을 걸어주고 count변수를 하나 선언해줍니다. 문자열을 정수형으로 바꿔주기 위해서는 str.charAt - '0'이라는 것을 사용하면
문자열에서 정수형으로 변환이 됩니다. 그리고 그 다음으로 count를 ++로 증감시켜줍니다.
```

> ⚡3052번 | 나머지
> ------------
>  풀이
```
<<<<<<< HEAD
문제에서 수를 10개 받는다고 하였으므로 배열의 크기가 10인 배열을 하나 생성해주고 서로 다른 나머지를
count해주는 count변수를 하나 선언해주고 똑같은 나머지가 있는 것을 세어주는 samecount 변수를 하나 선언해줍니다.
for문으로 배열 안의 값에 하나하나 값을 Scanner로 넣어주는데 배열의 값을 넣어 줄 때 42를 나눈 나머지의 값을
바로 넣어줍니다. 그 다음 이중 for문을 사용해주는데 바깥쪽 for문의 samecount = 0은 안쪽 for문을 돌고 아래의 
코드를 수행한 다음에 다시 바깥쪽 for문의 코드를 수행 할 때 다시 samecount를 0으로 초기화 시켜주는 것입니다.
=======
(풀이내용)
>>>>>>> parent of ee64aa0 (Delete 05_1차원 배열/김민성 directory)
```

> ⚡1546번 | 평균
> ------------
>  풀이
```
<<<<<<< HEAD
과목 갯수를 Scanner로 받아 주고 과목 수에 따른 동적 배열을 하나 생성해줍니다. 최댓값과 평균의 값이 필요하므로
int형 변수 max와 double형 변수 sum을 선언해주고 바로 초기화를 시켜줍니다. for문으로 배열 안에 값을 하나하나 
넣어주면서 최댓값을 찾아줍니다. 그 다음 문제의 조건에 따라 모든 점수 / 최댓값 * 100을 double형으로 계속
누적해줍니다. 마지막으로 print문에서 sum/number.length로 새로운 평균을 구해줍니다.
=======
(풀이내용)
>>>>>>> parent of ee64aa0 (Delete 05_1차원 배열/김민성 directory)
```

> ⚡8958번 | OX퀴즈
> ------------
>  풀이
```
<<<<<<< HEAD
문제에서 보면 x의 값은 0으로 고정되어있고 o의 값은 1부터 시작해서 x가 만나기 전까지 1씩 증감하는
형태입니다. 먼저 몇 개의 테스트를 받아주는 지를 Scanner로 받아주고 String형 str변수를 선언해줍니다.
for문으로 테스트의 갯수 길이 만큼 반복해주는데 Scanner로 ox를 입력받아주고 o가 얼마나 나오는지와
연속된 o에 따른 값들의 합을 저장하는 변수 sum을 선언해줍니다. 다음 for문에서 입력받은 ox의 길이만큼
반복해주는데 if문으로 입력받은 str이 O와 같다면 count를 하나 늘려주고 sum에 count의 값을 누적 합을 해줍니다.
반대로 만약 O가 아니라면 count를 0으로 초기화 시켜줍니다.
=======
(풀이내용)
>>>>>>> parent of ee64aa0 (Delete 05_1차원 배열/김민성 directory)
```

> ⚡4344번 | 평균은 넘겠지
> ------------
>  풀이
```
<<<<<<< HEAD
처음으로 전체 테스트 갯수를 배열로 만들어줍니다. 그 다음 각 테스트 마다 학생의 수를 받아주고
입력받은 학생의 수 만큼 점수를 입력받아줍니다. for문이 많이 있지만 순서대로 코드를 만들어줍니다.
가장 바깥쪽 for문은 입력받은 학생의 수와 마지막 for문이 돌고 다시 바깥쪽 for문으로 돌아오면 다시 새로운 
sum, avg,count의 값을 저장시켜줘야 하기 때문에 0으로 초기화를 시켜줍니다. 2번째 for문에는 각 학생의 점수를
입력받아주고 그 입력 받은 점수를 sum에 누적합 시켜줍니다. 그 다음 avg를 구해주고 if문으로 avg가 학생의 점수보다
적으면 count를 1 늘려줍니다. 출력 형식에 소수점 아래 3번째 까지 표현하라고 하였으므로 printf로 .3f로 소수점
아래 3번째까지 표현해줍니다.
=======
(풀이내용)
>>>>>>> parent of ee64aa0 (Delete 05_1차원 배열/김민성 directory)
```
